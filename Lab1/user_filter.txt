# 用户过滤规则配置文件
# 格式: allow/deny IP地址
#
# 示例：
# deny 192.168.1.100     # 阻止 192.168.1.100 访问代理
# allow 192.168.1.50     # 允许 192.168.1.50 访问代理
# deny 10.0.0.5          # 阻止 10.0.0.5 访问代理
#
# 规则说明：
# - 如果没有匹配的规则，默认允许访问
# - 必须精确匹配IP地址
# - 使用 # 开头的行为注释

# 示例配置（取消注释以启用）
# deny 127.0.0.2
# allow 127.0.0.1
